## AUTO-LOAD SESSION DECISIONS

First, run the SESSION_DECISIONS_SYSTEM auto-load:

```bash
bash ~/.claude/session-decisions-start.sh
```

This will automatically display:
- 📋 DECISION INDEX (all prior decisions)
- 📊 ACTIVE PROJECTS (current status)
- ⚠️ BLOCKERS (if any)

Then read ~/.claude/PERSONAL_MEMORY.md and ./CLAUDE_SESSION_LOG.md

Finally, tell me:
1. What we worked on last session (if any)
2. What was tried and what the outcomes were
3. Current status and next steps
4. Any relevant lessons from my personal memory
5. Review the auto-loaded decisions and confirm context is clear

If no prior session exists, just confirm you've read all files and are ready to work.
